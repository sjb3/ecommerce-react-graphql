import React from "react";
import EmailPassword from "../../components/EmailPassword";

export const Recovery = () => {
  return (
    <EmailPassword>
      <EmailPassword />
    </EmailPassword>
  );
};
